import { FC } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  background-color: #333b50;
`;

const CertificateItem: FC = () => (
  <Wrapper>Hello react</Wrapper>
);

export default CertificateItem;
